services:
  axopad:
    build: .
    container_name: axopad
    env_file: .env
    ports:
      - "3000:3000"
      - "5001:5001"
    restart: unless-stopped
    volumes:
      - ./backend/logs:/app/backend/logs
